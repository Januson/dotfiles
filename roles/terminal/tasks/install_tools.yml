---
- name: Install CLI tools
  dnf:
    name:
      # network utilization
      - bandwhich
      # cat clone
      - bat
      # git and diff viewer
      - delta
      # du alternative
      - dust
      # find alternative
      - fd
      # generates regexes from examples
      - grex
      # benchmarking
      - hyperfine
      # ls alternative
      - lsd
      # ps alternative
      - procs
      # grep alternative
      - ripgrep
      # search and replace
      - sd
      # tldr alternative
      - tealdeer
      # top alternative
      - ytop
      # cd alternative
      - zoxide
    state: latest
