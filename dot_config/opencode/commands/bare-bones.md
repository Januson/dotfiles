---
description: Strip file to bare bones — remove ai-known content, keep project-specific essence
---

Review the target file(s). Remove everything AI already knows — explanations of well-known concepts, redundant prose, obvious comments, boilerplate descriptions. Keep only what is project-specific, non-obvious, or acts as a precise constraint. Prefer references to authoritative sources over restating their content. Result must be minimal while preserving full intent and all essential guidance.
